{{#if hasControl}}
    <div id="stopScreenShare" class="ss" data-title="{{getString 'ssStop'}}">
      <button type="button"> {{getString 'ssBtn'}}</button>
    </div>
    <div id="screenController">
      <div class="share selfView">
        <button type="button">{{getString 'sharetoall'}}</button>
      </div>
    </div>
{{/if}}

